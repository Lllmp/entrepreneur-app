services:
  - type: web
    name: build-your-biz
    runtime: python
    buildCommand: ""
    startCommand: streamlit run app.py --server.port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
